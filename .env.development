# ============================================
# 개발 환경 변수 (Development Environment)
# ============================================
# 로컬 개발 시 사용되는 설정
#
# 사용 시점: yarn start:dev / yarn dev
#
# 용도:
# - 로컬 DB 연결 정보
# - 개발용 디버그 설정
# - 로컬 CORS 허용
# ============================================

# 환경 구분
NODE_ENV=development

# ============================================
# 데이터베이스 (PostgreSQL)
# ============================================
# 로컬 Docker 또는 직접 설치된 PostgreSQL
# ============================================

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=1234
DB_NAME=myapp

# TypeORM/Prisma 용 연결 문자열 (둘 중 하나 사용)
DATABASE_URL=postgresql://postgres:1234@localhost:5432/myapp

# DB 동기화 (개발에서만 true, 운영에서는 절대 true 금지)
DB_SYNCHRONIZE=true

# DB 로깅 (쿼리 로그 출력)
DB_LOGGING=true

# ============================================
# CORS 설정
# ============================================
# 프론트엔드 개발 서버 주소
# ============================================

CORS_ORIGIN=http://localhost:3000

# ============================================
# 로깅
# ============================================

LOG_LEVEL=debug
