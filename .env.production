# ============================================
# 운영 환경 변수 (Production Environment)
# ============================================
# 실제 운영 서버에 배포할 때 사용되는 설정
#
# 사용 시점: docker compose up (서버)
#
# 용도:
# - 운영 DB 연결 정보
# - 운영 보안 설정
# - 프로덕션 CORS
# ============================================

# 환경 구분
NODE_ENV=production

# ============================================
# 데이터베이스 (PostgreSQL)
# ============================================
# Docker Compose 내부 네트워크에서 컨테이너명으로 접근
# 호스트명 "postgres"는 docker-compose.yml의 서비스명
# ============================================

DB_HOST=postgres
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=1234
DB_NAME=myapp

# TypeORM/Prisma 용 연결 문자열
DATABASE_URL=postgresql://postgres:1234@postgres:5432/myapp

# DB 동기화 (운영에서는 반드시 false → 마이그레이션으로 관리)
DB_SYNCHRONIZE=false

# DB 로깅 (운영에서는 꺼두거나 error만)
DB_LOGGING=false

# ============================================
# CORS 설정
# ============================================
# 운영 프론트엔드 주소
# HTTP → 도메인 연결 후 HTTPS로 변경
# ============================================

CORS_ORIGIN=http://YOUR_SERVER_IP

# 도메인 연결 후:
# CORS_ORIGIN=https://your-domain.com

# ============================================
# 로깅
# ============================================

LOG_LEVEL=warn
